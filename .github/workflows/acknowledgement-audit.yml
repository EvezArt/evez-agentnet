name: acknowledgement-audit
on:
  push:
  pull_request:
jobs:
  acknowledgement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate acknowledgement records
        run: python3 tools/acknowledgement_audit.py docs/acknowledgements.jsonl
